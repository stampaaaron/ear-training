import { getRandomCadence } from "../../helper/candence";
import { Shell } from "../../layout/Shell";

export function Cadence() {

    console.log(getRandomCadence({}));
    

    return <Shell title="Cadence">

    </Shell>
}